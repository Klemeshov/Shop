let initialState = {
    findString: null,
    products: []
};

const FindReducer = (state = initialState, action) => {
    switch (action.type) {


        default:
            return state;
    }
};



export default FindReducer;